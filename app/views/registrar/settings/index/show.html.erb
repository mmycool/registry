<div class="page-header">
    <h1><%= t '.header' %></h1>
</div>

<% if ENV['auto_account_top_up'] == 'true' %>
    <div class="row">
        <div class="col-sm-6">
            <%= render 'registrar/settings/auto_account_top_up/for_settings', registrar: @registrar %>
        </div>
    </div>
<% end %>
